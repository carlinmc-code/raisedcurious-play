import{fitCanvas,shell,rand}from'../assets/common.js';shell('Wind Tunnel','Change the shape, angle, and wind. Watch lift and drag respond.');const c=document.querySelector('#game'),tb=document.querySelector('#toolbar'),co=document.querySelector('#controls'),st=document.querySelector('#status');let shape='wing',angle=8,wind=35;tb.innerHTML='<button data-s=wing class=active>Wing</button><button data-s=flat>Flat plate</button><button data-s=parachute>Parachute</button>';co.innerHTML='<div class=control><label>Angle <span id=av>8°</span></label><input id=a type=range min=-15 max=30 value=8></div><div class=control><label>Wind <span id=wv>35</span></label><input id=w type=range min=5 max=80 value=35></div>';tb.onclick=e=>{if(e.target.dataset.s){shape=e.target.dataset.s;[...tb.children].forEach(b=>b.classList.toggle('active',b.dataset.s===shape))}};document.querySelector('#a').oninput=e=>{angle=+e.target.value;document.querySelector('#av').textContent=angle+'°'};document.querySelector('#w').oninput=e=>{wind=+e.target.value;document.querySelector('#wv').textContent=wind};let particles=Array.from({length:80},()=>({x:rand(0,1),y:rand(0,1)}));function draw(){const{ctx,w,h}=fitCanvas(c,500);ctx.fillStyle='#e9f7ff';ctx.fillRect(0,0,w,h);ctx.strokeStyle='#5db7de';ctx.lineWidth=2;for(const p of particles){p.x+=wind/18000;if(p.x>1)p.x=0;let bend=Math.exp(-Math.pow((p.x-.52)*8,2))*Math.sin(angle*Math.PI/180)*.12;ctx.beginPath();ctx.moveTo(p.x*w,p.y*h);ctx.lineTo((p.x+.03)*w,(p.y-bend)*h);ctx.stroke()}ctx.save();ctx.translate(w*.52,h*.5);ctx.rotate(angle*Math.PI/180);ctx.fillStyle='#10243e';if(shape==='wing'){ctx.beginPath();ctx.ellipse(0,0,95,18,0,0,7);ctx.fill()}else if(shape==='flat')ctx.fillRect(-90,-6,180,12);else{ctx.font='100px system-ui';ctx.textAlign='center';ctx.fillText('🪂',0,25)}ctx.restore();let lift=shape==='wing'?Math.round(wind*angle*1.2):shape==='flat'?Math.round(wind*angle*.6):Math.round(wind*2);let drag=shape==='parachute'?wind*5:Math.round(wind*(shape==='flat'?1.7:.8));st.textContent=`Relative lift: ${lift} · drag: ${drag}. Try increasing angle until extra angle stops helping.`;requestAnimationFrame(draw)}requestAnimationFrame(draw);
